using UnityEngine;

public class VegetableStatus : MonoBehaviour
{
    public bool isSliced = false;   // Tracks if the vegetable has been sliced
    public bool hasSpawned = false; // Tracks if the vegetable was spawned
    public bool isActive = false;   // Tracks if the vegetable is visible on screen
}
